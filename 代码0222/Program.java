class Program {
	public static void main(String[] args) {
		// 变量
		// 全局变量 和 局部变量
		// 局部变量：声明在某一个代码段中的变量
		//			 方法中，if,else,switch,for,while,do....
		// 全局变量：声明在类中，和方法平级的变量
		// 
		// 局部变量：局部变量是有作用域的
		// 作用域：能够访问到这个变量的范围
		// 局部变量的作用域：从声明开始，到声明所在的代码段（大括号）结束
		// 两个同名的局部变量的作用域不允许重叠

		// 特殊：在for循环的循环起点位置声明的变量，作用域仅限于这个循环

		for (int i = 0; i < 10; i++) {
			
		}
	}
}